services:
  - type: web
    name: stockagent-india
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app --workers 1 --threads 2 --timeout 120
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
